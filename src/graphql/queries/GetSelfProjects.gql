query GetSelfProjects($where: ProjectWhereInput) {
  projects(where: $where) {
    id
    title
    description
    status_project
    users {
      avatar
      id
    }
    owner {
      first_name
      avatar
      last_name
      id
    }
  }
}
